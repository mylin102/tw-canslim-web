---
phase: 04-publishing-freshness-awareness
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - update_single_stock.py
  - .github/workflows/update_data.yml
  - .github/workflows/on_demand_update.yml
  - tests/test_operational_publish_path.py
  - tests/test_publish_workflows.py
autonomous: true
requirements: [PUB-02, PUB-03, PUB-04]

must_haves:
  truths:
    - "Scheduled runs no longer rely on a separate stock-index script outside the shared publish bundle."
    - "On-demand and scheduled flows both stage/commit Phase 4 artifacts consistently."
    - "Single-stock publishes rebuild the Phase 4 index/summary outputs instead of leaving them stale."
  artifacts:
    - path: "update_single_stock.py"
      provides: "Single-stock Phase 4 publish projection reuse"
      contains: "stock_index.json"
    - path: ".github/workflows/update_data.yml"
      provides: "Scheduled workflow aligned to Phase 4 bundle outputs"
      contains: "docs/stock_index.json"
    - path: ".github/workflows/on_demand_update.yml"
      provides: "On-demand workflow aligned to Phase 4 bundle outputs"
      contains: "docs/stock_index.json"
    - path: "tests/test_publish_workflows.py"
      provides: "Workflow regression coverage for Phase 4 artifact staging"
      contains: "stock_index.json"
  key_links:
    - from: "update_single_stock.py"
      to: "publish_projection.py"
      via: "shared projection helper reuse"
      pattern: "stock_index.json|update_summary.json"
    - from: ".github/workflows/update_data.yml"
      to: "export_canslim.py"
      via: "single primary publish step"
      pattern: "python export_canslim.py"
    - from: ".github/workflows/on_demand_update.yml"
      to: "update_single_stock.py"
      via: "single-stock publish path"
      pattern: "python3 update_single_stock.py"
---

<objective>
Align scheduled and on-demand automation with the Phase 4 artifact bundle so `stock_index.json` and the richer summary contract stay live after every supported publish path.

Purpose: Make the new outputs durable in CI and operational flows, not just in the main export module.
Output: Updated workflows, single-stock publish wiring, and workflow/operational regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-publishing-freshness-awareness/04-CONTEXT.md
@.planning/phases/04-publishing-freshness-awareness/04-RESEARCH.md
@update_single_stock.py
@.github/workflows/update_data.yml
@.github/workflows/on_demand_update.yml
@tests/test_operational_publish_path.py
@tests/test_publish_workflows.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regressions for Phase 4 artifact staging in workflow and single-stock publish paths</name>
  <files>tests/test_operational_publish_path.py, tests/test_publish_workflows.py</files>
  <read_first>
    - tests/test_operational_publish_path.py
    - tests/test_publish_workflows.py
    - update_single_stock.py
    - .github/workflows/update_data.yml
    - .github/workflows/on_demand_update.yml
    - .planning/phases/04-publishing-freshness-awareness/04-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `tests/test_operational_publish_path.py` contains an assertion for `stock_index.json`
    - `tests/test_publish_workflows.py` contains an assertion for `docs/stock_index.json`
    - `tests/test_publish_workflows.py` contains an assertion that `create_stock_index.py` is absent from the scheduled workflow path
    - `PYTHONPATH=. pytest -q tests/test_operational_publish_path.py tests/test_publish_workflows.py -x` exits 0
  </acceptance_criteria>
  <action>Add the workflow and operational regression scaffolding that locks in the Phase 4 automation contract: scheduled workflow no longer shells out to a standalone `create_stock_index.py`, both workflows stage `docs/stock_index.json` and `docs/update_summary.json`, and the single-stock publish path rebuilds/publishes those artifacts through the shared contract. Keep the assertions narrow and grep-verifiable so Task 2 can make the full suite pass after the workflow wiring lands.</action>
  <verify>
    <automated>grep -n "stock_index.json" tests/test_operational_publish_path.py && grep -n "docs/stock_index.json" tests/test_publish_workflows.py && grep -n "create_stock_index.py" tests/test_publish_workflows.py</automated>
  </verify>
  <done>Workflow and operational regression scaffolding exists so Task 2 can make the automation path pass green.</done>
</task>

<task type="auto">
  <name>Task 2: Reuse the Phase 4 projection helper in single-stock publishing and align both workflows</name>
  <files>update_single_stock.py, .github/workflows/update_data.yml, .github/workflows/on_demand_update.yml</files>
  <read_first>
    - update_single_stock.py
    - .github/workflows/update_data.yml
    - .github/workflows/on_demand_update.yml
    - export_canslim.py
    - publish_projection.py
    - .planning/phases/04-publishing-freshness-awareness/04-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `update_single_stock.py` contains `stock_index.json`
    - `.github/workflows/update_data.yml` does not contain `python create_stock_index.py`
    - `.github/workflows/update_data.yml` contains `docs/stock_index.json`
    - `.github/workflows/update_data.yml` contains `docs/update_summary.json`
    - `.github/workflows/on_demand_update.yml` contains `docs/stock_index.json`
    - `.github/workflows/on_demand_update.yml` contains `docs/update_summary.json`
    - `PYTHONPATH=. pytest -q tests/test_operational_publish_path.py tests/test_publish_workflows.py -x` exits 0
  </acceptance_criteria>
  <action>Update `update_single_stock.py` to reuse the shared Phase 4 projection helper from 04-01 so single-symbol publishes regenerate `data.json`, `stock_index.json`, and `update_summary.json` coherently instead of leaving index/summary stale. In the scheduled workflow, remove the separate `python create_stock_index.py` step because `export_canslim.py` now owns the bundle per PUB-03/PUB-04. In both workflows, stage and commit `docs/stock_index.json` and `docs/update_summary.json` explicitly. Preserve the existing publish-surface concurrency and rotation-state restore/upload behavior from Phase 1/3.</action>
  <verify>
    <automated>PYTHONPATH=. pytest -q tests/test_operational_publish_path.py tests/test_publish_workflows.py -x && grep -n "docs/stock_index.json" .github/workflows/update_data.yml .github/workflows/on_demand_update.yml</automated>
  </verify>
  <done>Every supported automated publish path leaves Phase 4 artifacts synchronized and committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub Actions steps → repo artifacts | Workflow commands determine which generated files reach git and Pages |
| single-stock updater → shared publish bundle | Operational path can drift from the primary export contract if not reused |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-08 | T | `update_single_stock.py` | mitigate | Reuse the shared Phase 4 projection helper instead of duplicating index/summary generation logic |
| T-04-09 | R | GitHub workflow staging | mitigate | Add tests asserting `docs/stock_index.json` and `docs/update_summary.json` are staged in both workflows |
| T-04-10 | D | scheduled workflow | mitigate | Remove the standalone `create_stock_index.py` dependency so publish does not fail on a missing/out-of-sync extra script |
</threat_model>

<verification>
Run the targeted workflow/operational pytest command, then grep both workflow files for the staged Phase 4 artifacts and absence of the removed standalone index step.
</verification>

<success_criteria>
- `update_single_stock.py` regenerates Phase 4 projection outputs
- Scheduled workflow no longer calls `create_stock_index.py`
- Both workflows commit `stock_index.json` and `update_summary.json`
</success_criteria>

<output>
After completion, create `.planning/phases/04-publishing-freshness-awareness/04-03-SUMMARY.md`
</output>
