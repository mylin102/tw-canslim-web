---
phase: 04-publishing-freshness-awareness
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - docs/app.js
  - docs/index.html
autonomous: true
requirements: [PUB-01, PUB-02, PUB-03]

must_haves:
  truths:
    - "User sees freshness badges in search suggestions, stock detail, ranking rows, and screener rows."
    - "User can search by stock code or name substring across the full published stock index."
    - "User can open a non-snapshot stock result and is told that full CANSLIM detail is unavailable."
  artifacts:
    - path: "docs/app.js"
      provides: "Index-backed search, freshness helpers, and partial-detail fallback"
      contains: "stock_index.json"
    - path: "docs/index.html"
      provides: "Freshness badge rendering and non-snapshot detail notice"
      contains: "CANSLIM 明細未收錄於本次快照"
  key_links:
    - from: "docs/app.js"
      to: "stock_index.json"
      via: "fetch on initial load"
      pattern: "fetch\\('stock_index.json"
    - from: "docs/app.js"
      to: "docs/index.html"
      via: "freshness.label bindings"
      pattern: "freshness"
    - from: "docs/app.js"
      to: "data.json"
      via: "snapshot hydration for full detail"
      pattern: "data.json"
---

<objective>
Rewire the dashboard to consume the Phase 4 publish contract: full-universe search via `stock_index.json`, freshness badges on all requested surfaces, and explicit partial-detail handling for non-snapshot stocks.

Purpose: Deliver the user-facing half of PUB-01, PUB-02, and PUB-03 without rewriting the frontend architecture.
Output: Updated `docs/app.js` and `docs/index.html`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-publishing-freshness-awareness/04-CONTEXT.md
@.planning/phases/04-publishing-freshness-awareness/04-RESEARCH.md
@docs/app.js
@docs/index.html

<interfaces>
Expected payloads from 04-01:
```json
{
  "stocks": {
    "2330": {
      "symbol": "2330",
      "name": "台積電",
      "freshness": { "level": "today", "label": "🟢 今日", "days_old": 0 },
      "last_succeeded_at": "2026-04-19T02:00:05Z"
    }
  }
}
```

```json
{
  "entries": [
    {
      "symbol": "1101",
      "name": "台泥",
      "industry": "水泥",
      "freshness": { "level": "days_1_2", "label": "🟡 2天前", "days_old": 2 },
      "last_succeeded_at": "2026-04-17T02:00:05Z",
      "in_snapshot": false
    }
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load stock index alongside snapshot data and switch search to full-universe matching</name>
  <files>docs/app.js</files>
  <read_first>
    - docs/app.js
    - docs/index.html
    - .planning/phases/04-publishing-freshness-awareness/04-CONTEXT.md
    - .planning/phases/04-publishing-freshness-awareness/04-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `docs/app.js` contains `fetch('stock_index.json`
    - `docs/app.js` contains `Promise.all(`
    - `docs/app.js` contains `entry.in_snapshot`
    - `docs/app.js` contains substring matching on both `symbol` and `name`
    - `node --check docs/app.js` exits 0
  </acceptance_criteria>
  <action>Update the Vue setup to fetch `data.json` and `stock_index.json` together, then keep `data.json` as the snapshot-detail source per D-09 while using `stock_index.json` for search suggestions per D-05/D-08. Replace the current suggestion builder that only scans `stockData.stocks` with index-backed matching on stock code and lowercase stock name substring. Add an explicit result model that returns full snapshot detail when `in_snapshot === true`, otherwise returns a lightweight object with identity, freshness, `last_succeeded_at`, and `has_full_detail: false` per D-06. Do not add new search facets or industry filters inside the global search box; deferred ideas stay deferred.</action>
  <verify>
    <automated>node --check docs/app.js && grep -n "stock_index.json" docs/app.js && grep -n "in_snapshot" docs/app.js</automated>
  </verify>
  <done>Search suggestions and selected search results come from the full published index while snapshot detail still hydrates from `data.json` when available.</done>
</task>

<task type="auto">
  <name>Task 2: Render freshness badges and explicit non-snapshot fallback across all requested UI surfaces</name>
  <files>docs/app.js, docs/index.html</files>
  <read_first>
    - docs/app.js
    - docs/index.html
    - .planning/phases/04-publishing-freshness-awareness/04-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `docs/index.html` contains `CANSLIM 明細未收錄於本次快照`
    - `docs/index.html` contains freshness rendering in the search suggestion block
    - `docs/index.html` contains freshness rendering in the stock detail header
    - `docs/index.html` contains freshness rendering in the ranking table
    - `docs/index.html` contains freshness rendering in the screener table
    - `docs/app.js` contains a freshness badge helper that branches on `today`, `days_1_2`, and `days_3_plus`
    - `node --check docs/app.js` exits 0
  </acceptance_criteria>
  <action>Add freshness badge helpers in `docs/app.js` and render them in `docs/index.html` per D-03/D-04 on these exact surfaces: search suggestions, stock detail header, ranking rows, and screener rows. Use the Phase 4 labels directly (`🟢 今日`, `🟡 {n}天前`, `🔴 逾3天`). For non-snapshot stocks per D-06, keep the stock identity block visible but replace the CANSLIM/institutional/detail sections with a clear message: `CANSLIM 明細未收錄於本次快照，請參考基本資料與新鮮度標示。` Do not fake scores, PASS/FAIL states, or institutional tables for `has_full_detail: false` results.</action>
  <verify>
    <automated>node --check docs/app.js && grep -n "CANSLIM 明細未收錄於本次快照" docs/index.html && grep -n "🟢 今日" docs/app.js && grep -n "逾3天" docs/app.js</automated>
  </verify>
  <done>The active dashboard shows freshness everywhere the user asked, and non-snapshot results are explicit about limited detail instead of pretending full coverage exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| published JSON → browser UI | Untrusted static payload fields are rendered to users |
| search input → client-side matching | Arbitrary user text drives full-universe search behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-05 | I | `docs/index.html` detail view | mitigate | Show explicit limited-detail state for `in_snapshot: false` results so missing CANSLIM data is never misrepresented |
| T-04-06 | T | `docs/app.js` freshness display | mitigate | Render only server-provided freshness labels derived in 04-01; do not recalculate from `lastUpdated` in the browser |
| T-04-07 | D | client search | accept | Substring search on a static JSON index is low risk and bounded to local browser memory; no external query execution |
</threat_model>

<verification>
Load the page locally after 04-01 data exists, verify search suggestions come from `stock_index.json`, and confirm the grep/node checks pass.
</verification>

<success_criteria>
- Search suggestions are full-universe, not snapshot-only
- Search, detail, ranking, and screener surfaces all show freshness badges
- Non-snapshot results show a limited-detail notice instead of CANSLIM placeholders
</success_criteria>

<output>
After completion, create `.planning/phases/04-publishing-freshness-awareness/04-02-SUMMARY.md`
</output>
