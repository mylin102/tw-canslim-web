---
phase: 02-dynamic-core-selection
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - historical_generator.py
  - fuse_excel_data.py
  - core_selection.py
  - tests/test_core_selection.py
autonomous: true
requirements:
  - ORCH-01
must_haves:
  truths:
    - "Today and yesterday signal buckets come from the trusted fused parquet path, not a second parser."
    - "Volume leaders are derived from persisted signal artifacts carrying a latest volume metric, not a daily live prepass."
    - "The selector guarantees explicit RS-leader and top-volume-leader inclusion before any remaining ranked fill, then returns a deterministic ~300-name core list."
  artifacts:
    - path: historical_generator.py
      provides: "Master signal parquet includes persisted latest-volume ranking inputs."
    - path: fuse_excel_data.py
      provides: "Fused signal parquet keeps latest-volume fields alongside alpha columns."
    - path: core_selection.py
      provides: "Artifact-backed loader and deterministic core-universe builder."
    - path: tests/test_core_selection.py
      provides: "Executable proof for D-02 through D-06 selector behavior."
  key_links:
    - from: historical_generator.py
      to: master_canslim_signals.parquet
      via: "persisted `latest_volume` and `volume_rank` columns"
      pattern: "latest_volume|volume_rank"
    - from: fuse_excel_data.py
      to: master_canslim_signals_fused.parquet
      via: "merge-through of volume fields"
      pattern: "latest_volume|volume_rank"
    - from: core_selection.py
      to: alpha_integration_module.py
      via: "same score>=75 semantics and fused-date signal sourcing"
      pattern: "score|date|75"
---

<objective>
Implement the artifact-backed selector and persist the volume inputs it needs.

Purpose: fulfill the locked decisions around trusted fused signals, one-day carryover, fixed-bucket precedence, and persisted volume leaders without introducing Phase 3 orchestration state or a live daily prepass.
Output: master/fused parquet artifacts carry selector-ready volume fields, and `build_core_universe(...)` returns the ordered core list plus bucket metadata.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dynamic-core-selection/02-CONTEXT.md
@.planning/phases/02-dynamic-core-selection/02-RESEARCH.md
@.planning/phases/02-dynamic-core-selection/02-VALIDATION.md
@historical_generator.py
@fuse_excel_data.py
@core_selection.py
@alpha_integration_module.py
@tests/test_core_selection.py

<interfaces>
Contract from 02-01 to implement:

```python
SIGNAL_SCORE_THRESHOLD = 75

def load_selector_inputs(
    *,
    config_path: str,
    fused_path: str,
    master_path: str,
    baseline_path: str,
) -> ...

def build_core_universe(...) -> CoreSelectionResult
```

Current fused-path semantics to preserve:

```python
df_merged['is_canslim_approved'] = df_merged['score'].fillna(0) >= min_score
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist latest-volume metrics into the master/fused signal artifacts</name>
  <files>historical_generator.py, fuse_excel_data.py, tests/test_core_selection.py</files>
  <read_first>
    - .planning/phases/02-dynamic-core-selection/02-CONTEXT.md
    - .planning/phases/02-dynamic-core-selection/02-RESEARCH.md
    - historical_generator.py
    - fuse_excel_data.py
    - tests/test_core_selection.py
  </read_first>
  <behavior>
    - Test 1: latest market rows persisted by `historical_generator.py` include `latest_volume` sourced from `Trading_Volume`.
    - Test 2: each trading date also carries a deterministic `volume_rank` so D-06 can be satisfied without live fetches.
    - Test 3: `fuse_excel_data.py` copies `latest_volume` and `volume_rank` into `master_canslim_signals_fused.parquet`.
  </behavior>
  <action>Extend `historical_generator.py` so `process_ticker(...)` returns `latest_volume` from `Trading_Volume`, then compute a date-level `volume_rank` after concatenation using descending rank where rank `1` is highest volume. Persist both columns into `master_canslim_signals.parquet`. Update `fuse_excel_data.py` to merge-through `latest_volume` and `volume_rank` alongside `rs_rating`, `composite_rating`, `smr_rating`, and `fund_change`; do not add any yfinance-based prepass in this plan because D-06 requires artifact-driven volume leaders. Add or extend tests in `tests/test_core_selection.py` so they assert the persisted volume columns exist and are used as selector inputs.</action>
  <acceptance_criteria>
    - `grep -q "latest_volume" historical_generator.py`
    - `grep -q "volume_rank" historical_generator.py`
    - `grep -q "latest_volume" fuse_excel_data.py`
    - `PYTHONPATH=. pytest -q tests/test_core_selection.py -k volume`
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest -q tests/test_core_selection.py -k volume</automated>
  </verify>
  <done>The persisted master/fused signal artifacts carry latest-volume ranking fields, and Phase 2 no longer depends on a live daily volume prepass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement artifact-backed core-universe selection with freshness guards</name>
  <files>core_selection.py, tests/test_core_selection.py</files>
  <read_first>
    - core_selection.py
    - core_selection_config.json
    - alpha_integration_module.py
    - historical_generator.py
    - fuse_excel_data.py
    - tests/test_core_selection.py
  </read_first>
  <behavior>
    - Test 1: fixed buckets from config are ordered `base -> ETF -> watchlist -> yesterday signals -> today signals` and deduped by first appearance per D-01 and D-04.
    - Test 2: today signals are pulled from the latest fused parquet date with `score >= 75`, yesterday carryover uses only the immediately previous fused date, and stale fused data is rejected if master parquet is newer per D-02 and D-05.
    - Test 3: an explicit `rs_leaders` bucket includes every symbol with `mansfield_rs > 80`, and an explicit `top_volume_leaders` bucket includes the top 100 symbols by persisted `volume_rank`, both before final dedupe/cap per ORCH-01.
    - Test 4: remaining slots fill up to `target_size=300` by deterministic sort `(-mansfield_rs, volume_rank, symbol)` after all required buckets are assembled, using persisted volume fields per D-03, D-04, and D-06.
  </behavior>
  <action>Implement `load_core_selection_config(...)`, `load_selector_inputs(...)`, and `build_core_universe(...)` in `core_selection.py`. Use `core_selection_config.json` as the source for `base_symbols`, `etf_symbols`, and `watchlist_symbols`; validate that each symbol is a 4-digit string before use. Load `master_canslim_signals_fused.parquet` and `master_canslim_signals.parquet`, require columns `stock_id`, `date`, `score`, `latest_volume`, and `volume_rank`, and fail closed with an explicit `ValueError` when the fused latest date is older than the master latest date. Derive `today_signals` and `yesterday_signals` from the latest two fused dates using `score >= SIGNAL_SCORE_THRESHOLD` to match AlphaFilter semantics from D-02. Load `docs/data_base.json` RS values from `stocks.*.canslim.mansfield_rs`, then build these explicit selector buckets before dedupe and cap: `rs_leaders` = every symbol with `mansfield_rs > 80`, and `top_volume_leaders` = the top 100 symbols by ascending `volume_rank` (rank 1 = highest volume). Assemble the ordered bucket stream exactly as `base_symbols -> etf_symbols -> watchlist_symbols -> yesterday_signals -> today_signals -> rs_leaders -> top_volume_leaders`, dedupe by first appearance, and only then fill any leftover slots with the exact stable sort key `(-mansfield_rs, volume_rank, symbol)`. Return bucket membership counts, `core_symbols`, and `core_set`; do not add Phase 3 rotation outputs or Phase 4 publish artifacts.</action>
  <acceptance_criteria>
    - `grep -q "SIGNAL_SCORE_THRESHOLD = 75" core_selection.py`
    - `grep -q "mansfield_rs > 80" core_selection.py`
    - `grep -q "top_volume_leaders" core_selection.py`
    - `grep -q "volume_rank" core_selection.py`
    - `grep -q "mansfield_rs" core_selection.py`
    - `PYTHONPATH=. pytest -q tests/test_core_selection.py`
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest -q tests/test_core_selection.py</automated>
  </verify>
  <done>`build_core_universe(...)` produces a deterministic, artifact-driven core list that satisfies D-01 through D-06 without introducing rotation/state or publish-surface scope creep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| master/fused parquet→selector | Persisted market artifacts become daily priority inputs |
| baseline JSON→selector ranking | Cached RS values influence which symbols enter the 300-name core |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | `master_canslim_signals_fused.parquet` freshness | mitigate | Compare fused latest date to master latest date and fail closed when fused is stale. |
| T-02-05 | T | selector input schema | mitigate | Require `stock_id`, `date`, `score`, `latest_volume`, and `volume_rank` before ranking. |
| T-02-06 | D | core-universe cap | mitigate | Enforce `target_size=300` after fixed buckets and record overflow/remaining counts in the result object. |
</threat_model>

<verification>
Run `python3 -m py_compile historical_generator.py fuse_excel_data.py core_selection.py` and `PYTHONPATH=. pytest -q tests/test_core_selection.py`.
</verification>

<success_criteria>
- Volume leaders come from persisted artifact columns, not live fetches.
- Same-day and one-day carryover signals come from the fused parquet path with explicit freshness validation.
- The selector returns deterministic ordering for fixed buckets and ranked fill up to 300 names.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dynamic-core-selection/02-02-SUMMARY.md`
</output>
