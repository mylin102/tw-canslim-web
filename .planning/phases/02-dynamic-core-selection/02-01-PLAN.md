---
phase: 02-dynamic-core-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core_selection.py
  - core_selection_config.json
  - tests/conftest.py
  - tests/test_core_selection.py
  - core/logic.py
autonomous: true
requirements:
  - ORCH-01
must_haves:
  truths:
    - "Maintainer has a checked-in selector config for base, ETF, and watchlist buckets instead of editing `export_canslim.py`."
    - "Selector behavior for fixed buckets, one-day carryover, and the ~300-name cap is executable under pytest before export wiring starts."
    - "Phase 2 targeted pytest commands collect cleanly in this repo."
  artifacts:
    - path: core_selection.py
      provides: "Exported selector/config/result contracts for downstream implementation."
    - path: core_selection_config.json
      provides: "Checked-in base_symbols, etf_symbols, watchlist_symbols, and target_size seed config."
    - path: tests/test_core_selection.py
      provides: "Selector behavior coverage for D-01, D-03, D-04, D-05, and D-06 expectations."
    - path: tests/conftest.py
      provides: "Synthetic selector fixtures for fused/master/baseline inputs."
    - path: core/logic.py
      provides: "Backwards-compatible institutional helper so pytest collection is green."
  key_links:
    - from: core_selection_config.json
      to: core_selection.py
      via: "load_core_selection_config"
      pattern: "base_symbols|etf_symbols|watchlist_symbols|target_size"
    - from: tests/test_core_selection.py
      to: core_selection.py
      via: "imported selector contracts and fixtures"
      pattern: "build_core_universe|CoreSelectionConfig|CoreSelectionResult"
    - from: tests/test_institutional_logic.py
      to: core/logic.py
      via: "calculate_i_factor import"
      pattern: "def calculate_i_factor"
---

<objective>
Establish the brownfield selector contract, checked-in config, and Wave 0 test harness for dynamic core selection.

Purpose: remove the hard-coded-list setup cost before implementation, and unblock Phase 2 verification without touching Phase 3 rotation/state or Phase 4 publishing work.
Output: selector contract file, curated config seed, selector fixture/test scaffold, and green collection for targeted/full-suite pytest entry points.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dynamic-core-selection/02-CONTEXT.md
@.planning/phases/02-dynamic-core-selection/02-RESEARCH.md
@.planning/phases/02-dynamic-core-selection/02-VALIDATION.md
@update_strategy.md
@export_canslim.py
@alpha_integration_module.py
@tests/conftest.py
@tests/test_institutional_logic.py
@core/logic.py

<interfaces>
Current brownfield seam to replace later:

```python
# export_canslim.py
priority = ["1101", "2330", "3565", "6770", "2303", "8069", "6805"]
all_t = sorted(list(self.ticker_info.keys()))
scan_list = priority + [t for t in all_t if t not in priority][:2000]
```

Signal approval source already in repo:

```python
# alpha_integration_module.py
df_merged['is_canslim_approved'] = df_merged['score'].fillna(0) >= min_score
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Write selector contracts and checked-in bucket config</name>
  <files>core_selection.py, core_selection_config.json</files>
  <read_first>
    - .planning/phases/02-dynamic-core-selection/02-CONTEXT.md
    - .planning/phases/02-dynamic-core-selection/02-RESEARCH.md
    - export_canslim.py
    - alpha_integration_module.py
    - core_selection.py
    - core_selection_config.json
  </read_first>
  <action>Create `core_selection.py` as a contract-first module with exported `SIGNAL_SCORE_THRESHOLD = 75`, `CoreSelectionConfig`, `RankedCandidate`, and `CoreSelectionResult` dataclasses plus function signatures for `load_core_selection_config(...)`, `load_selector_inputs(...)`, and `build_core_universe(...)`. In `core_selection_config.json`, store a checked-in seed config for D-01/D-03/D-04 using exact keys `base_symbols`, `etf_symbols`, `watchlist_symbols`, and `target_size`; seed `base_symbols` with `["1101","2330","2303","3565","6770","6805","8069"]`, seed `etf_symbols` with `["0050","0056","00878"]`, keep `watchlist_symbols` as an editable array, and set `target_size` to `300` per D-03. Do not put any bucket list back inside `export_canslim.py`.</action>
  <acceptance_criteria>
    - `python3 -c "import json; cfg=json.load(open('core_selection_config.json')); assert cfg['target_size']==300; assert cfg['base_symbols'][0]=='1101'; assert '0050' in cfg['etf_symbols']"`
    - `grep -q "class CoreSelectionConfig" core_selection.py`
    - `grep -q "def build_core_universe" core_selection.py`
  </acceptance_criteria>
  <verify>
    <automated>python3 -m py_compile core_selection.py && python3 -c "import json; cfg=json.load(open('core_selection_config.json')); assert sorted(cfg.keys()) == ['base_symbols', 'etf_symbols', 'target_size', 'watchlist_symbols']"</automated>
  </verify>
  <done>`core_selection.py` exports the selector contracts, and `core_selection_config.json` contains the brownfield bucket source-of-truth with a 300-name target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add selector fixtures/tests and restore pytest collection for Phase 2</name>
  <files>tests/conftest.py, tests/test_core_selection.py, core/logic.py</files>
  <read_first>
    - .planning/phases/02-dynamic-core-selection/02-VALIDATION.md
    - tests/conftest.py
    - tests/test_institutional_logic.py
    - core/logic.py
    - core_selection.py
    - tests/test_core_selection.py
  </read_first>
  <behavior>
    - Test 1: fixed buckets are deduped by first appearance and stay ahead of ranked fill per D-01 and D-04.
    - Test 2: today signals come from the latest fused parquet date and yesterday carryover lasts exactly one prior fused date per D-02 and D-05.
    - Test 3: selector rejects stale fused inputs when master parquet is newer, and volume leaders require persisted `latest_volume` / `volume_rank` fields per D-06.
    - Test 4: `tests/test_institutional_logic.py` imports `calculate_i_factor` successfully under `PYTHONPATH=.`.
  </behavior>
  <action>Extend `tests/conftest.py` with deterministic factories that build synthetic selector inputs: temp `master_canslim_signals.parquet`, temp `master_canslim_signals_fused.parquet`, temp `docs/data_base.json`, and temp config payloads. Create `tests/test_core_selection.py` with grouped pytest cases named around `fixed_buckets`, `signals`, `ranking`, and `stale_fused`. Add a backwards-compatible `calculate_i_factor(chip_df, days=3, total_shares=None, conviction_threshold=0.002)` helper to `core/logic.py` so the existing institutional test file collects and passes; use the existing `calculate_accumulation_strength(...)` semantics instead of inventing a new formula. Keep all Phase 2 verify commands on `PYTHONPATH=.` exactly as required by VALIDATION.md.</action>
  <acceptance_criteria>
    - `test -f tests/test_core_selection.py`
    - `grep -q "def test_.*fixed_buckets" tests/test_core_selection.py`
    - `grep -q "def calculate_i_factor" core/logic.py`
    - `PYTHONPATH=. pytest -q tests/test_institutional_logic.py tests/test_core_selection.py --collect-only`
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest -q tests/test_institutional_logic.py tests/test_core_selection.py --collect-only</automated>
  </verify>
  <done>The repo has executable selector test scaffolding, and Phase 2 targeted pytest collection no longer fails on the pre-existing `calculate_i_factor` gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in config→selector contract | Curated symbol lists enter the daily priority path |
| test fixtures→selector implementation | Synthetic parquet/JSON fixtures define what future selector code is allowed to trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `core_selection_config.json` | mitigate | Validate required keys, integer target size, and 4-digit symbol strings before selector use. |
| T-02-02 | D | selector bucket config | mitigate | Keep `target_size=300` in checked-in config and test overflow behavior before any integration. |
| T-02-03 | T | selector fixtures | mitigate | Build deterministic parquet/JSON fixtures in pytest so future logic is constrained to validated columns and dates. |
</threat_model>

<verification>
Run `python3 -m py_compile core_selection.py core/logic.py` and `PYTHONPATH=. pytest -q tests/test_institutional_logic.py tests/test_core_selection.py --collect-only`.
</verification>

<success_criteria>
- Selector contracts and config exist outside `export_canslim.py`.
- Phase 2 test scaffolding covers fixed buckets, carryover, ranking, and stale fused input handling.
- The existing institutional test import no longer blocks Phase 2 verification commands.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dynamic-core-selection/02-01-SUMMARY.md`
</output>
