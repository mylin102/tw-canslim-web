---
phase: 02-dynamic-core-selection
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - export_canslim.py
  - tests/test_primary_publish_path.py
autonomous: true
requirements:
  - ORCH-01
must_haves:
  truths:
    - "The daily CANSLIM export uses the dynamic core selector instead of a hard-coded priority list."
    - "The core-first scan still preserves Phase 1 publish safety and explicit failure summaries."
    - "Non-core names remain in the brownfield scan tail without pulling rotation/state work into this phase."
  artifacts:
    - path: export_canslim.py
      provides: "Dynamic core-selection wiring for the daily scan list."
    - path: tests/test_primary_publish_path.py
      provides: "Integration coverage proving export_canslim consumes selector output and preserves bundle-safe publishing."
  key_links:
    - from: export_canslim.py
      to: core_selection.py
      via: "build_core_universe(...)"
      pattern: "build_core_universe|core_selection_config.json"
    - from: export_canslim.py
      to: publish_safety.py
      via: "_publish_snapshot"
      pattern: "publish_artifact_bundle|update_summary"
---

<objective>
Replace the hard-coded priority seam in `export_canslim.py` with the artifact-backed core selector.

Purpose: finish ORCH-01 in the existing export entry point while keeping Phase 1 publish protections intact and leaving Phase 3 rotation/state plus Phase 4 publishing features out of scope.
Output: `export_canslim.py` builds `scan_list` from `build_core_universe(...)`, and regression tests prove the selector output drives scan order without breaking bundle-safe publishing.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dynamic-core-selection/02-CONTEXT.md
@.planning/phases/02-dynamic-core-selection/02-VALIDATION.md
@core_selection.py
@core_selection_config.json
@export_canslim.py
@tests/test_primary_publish_path.py

<interfaces>
Phase 2 selector contract to consume:

```python
selection = build_core_universe(
    all_symbols=sorted(self.ticker_info.keys()),
    config_path=os.path.join(SCRIPT_DIR, "core_selection_config.json"),
    fused_path=os.path.join(SCRIPT_DIR, "master_canslim_signals_fused.parquet"),
    master_path=os.path.join(SCRIPT_DIR, "master_canslim_signals.parquet"),
    baseline_path=os.path.join(OUTPUT_DIR, "data_base.json"),
)
scan_list = selection.core_symbols + [t for t in all_t if t not in selection.core_set][:2000]
```

Publish path that must remain intact:

```python
bundle = {
    DATA_FILE: {"artifact_kind": "data", "payload": self.output_data},
    update_summary_file: {"artifact_kind": "update_summary", "payload": self._build_update_summary()},
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add export-level regression coverage for dynamic core selection</name>
  <files>tests/test_primary_publish_path.py</files>
  <read_first>
    - tests/test_primary_publish_path.py
    - export_canslim.py
    - core_selection.py
    - core_selection_config.json
  </read_first>
  <behavior>
    - Test 1: `export_canslim.py` calls `build_core_universe(...)` and iterates `selection.core_symbols` first instead of the old inline `priority` list.
    - Test 2: the brownfield tail still appends non-core tickers after the selector core without duplicating symbols.
    - Test 3: publish summary bundle behavior from Phase 1 still occurs after selector integration.
  </behavior>
  <action>Extend `tests/test_primary_publish_path.py` with a focused `export_canslim` case that monkeypatches `build_core_universe(...)` to return a known ordered core such as `["0050","2330","1101"]`, then asserts the engine processes that order first and still publishes `docs/data.json` plus `docs/update_summary.json` through the existing bundle helper. Keep the test isolated to the scan-list seam; do not add new publishing assertions unrelated to selector wiring.</action>
  <acceptance_criteria>
    - `grep -q "build_core_universe" tests/test_primary_publish_path.py`
    - `PYTHONPATH=. pytest -q tests/test_primary_publish_path.py -k export_canslim`
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest -q tests/test_primary_publish_path.py -k export_canslim</automated>
  </verify>
  <done>The export integration test fails if `export_canslim.py` falls back to the old inline priority behavior or breaks the Phase 1 publish bundle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire `export_canslim.py` to the selector and keep Phase 1 publish safety intact</name>
  <files>export_canslim.py, tests/test_primary_publish_path.py</files>
  <read_first>
    - export_canslim.py
    - core_selection.py
    - core_selection_config.json
    - .planning/phases/01-safety-hardening/01-02-SUMMARY.md
    - tests/test_primary_publish_path.py
  </read_first>
  <behavior>
    - Test 1: `build_core_universe(...)` replaces the hard-coded `priority`/`scan_list` seam and uses the checked-in config plus artifact paths for D-01 through D-06.
    - Test 2: if selector inputs are stale or invalid, `export_canslim.py` fails loudly instead of silently reintroducing static priorities, preserving D-02 trust semantics.
    - Test 3: `_publish_snapshot()` and failure summary logic remain unchanged from Phase 1.
  </behavior>
  <action>Import `build_core_universe` from `core_selection.py` inside `export_canslim.py` and replace the inline `priority` / `scan_list` block with a selector call that passes `all_symbols`, `core_selection_config.json`, `master_canslim_signals_fused.parquet`, `master_canslim_signals.parquet`, and `docs/data_base.json`. Build the final `scan_list` exactly as `selection.core_symbols + [t for t in all_t if t not in selection.core_set][:2000]` so the brownfield fallback tail stays intact without adding Phase 3 rotation logic. Log the returned bucket counts and final core size, and let selector freshness/schema errors surface as explicit run failures; do not alter `_publish_snapshot()`, update-summary generation, or other Phase 1 publish-safety behavior.</action>
  <acceptance_criteria>
    - `grep -q "build_core_universe" export_canslim.py`
    - `! git --no-pager grep -n "priority = \\[" -- export_canslim.py`
    - `PYTHONPATH=. pytest -q tests/test_primary_publish_path.py -k export_canslim`
  </acceptance_criteria>
  <verify>
    <automated>python3 -m py_compile export_canslim.py core_selection.py && PYTHONPATH=. pytest -q tests/test_primary_publish_path.py -k export_canslim</automated>
  </verify>
  <done>`export_canslim.py` no longer owns a static priority list, and the daily scan order now comes from the dynamic selector while Phase 1 publish safety remains untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selector result→export loop | Ordered symbols determine which stocks are refreshed first |
| export loop→publish bundle | Selector integration must not break validated live artifact publishing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | T | `export_canslim.py` selector inputs | mitigate | Pass only checked-in config and persisted artifact paths into `build_core_universe(...)`; fail loudly on selector validation errors. |
| T-02-08 | D | scan-order seam | mitigate | Keep the non-core tail bounded to `[:2000]` after the selector-produced core set. |
| T-02-09 | R | publish summary path | mitigate | Leave `_publish_snapshot()` and update-summary generation on the Phase 1 bundle-safe helper unchanged. |
</threat_model>

<verification>
Run `python3 -m py_compile export_canslim.py core_selection.py` and `PYTHONPATH=. pytest -q tests/test_primary_publish_path.py -k export_canslim`.
</verification>

<success_criteria>
- `export_canslim.py` builds its core-first scan order from the new selector.
- Static priority literals are removed from the export path.
- Phase 1 bundle-safe publish behavior continues to pass the existing `export_canslim` regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dynamic-core-selection/02-03-SUMMARY.md`
</output>
