---
phase: 01-safety-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - export_canslim.py
  - export_dashboard_data.py
  - tests/test_primary_publish_path.py
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - SAFE-04
must_haves:
  truths:
    - "Primary exporters publish validated artifacts through the bundle-safe helper instead of raw live-file writes."
    - "Resume skips only schema-compatible stock records; incomplete older records are explicitly rebuilt."
    - "Retry attempts, suppressed-provider failures, and final publish status are surfaced in explicit failure summaries."
    - "A failed primary publish leaves the last validated bundle restorable."
  artifacts:
    - path: export_canslim.py
      provides: "Main CANSLIM export uses bundle-safe publish, record-level resume validation, and failure summary reporting."
    - path: export_dashboard_data.py
      provides: "Dashboard export uses artifact-aware validation instead of a stock-only helper assumption."
    - path: tests/test_primary_publish_path.py
      provides: "Smoke/regression coverage for primary export scripts."
  key_links:
    - from: export_canslim.py
      to: publish_safety.py
      via: "load_artifact_json + validate_resume_stock_entry + publish_artifact_bundle"
      pattern: "load_artifact_json|validate_resume_stock_entry|publish_artifact_bundle"
    - from: export_canslim.py
      to: docs/update_summary.json
      via: "failure summary payload in bundle publish"
      pattern: "update_summary|retry|resume_rejected"
    - from: export_dashboard_data.py
      to: publish_safety.py
      via: "artifact-aware publish"
      pattern: "validate_artifact_payload|publish_artifact_bundle|artifact_kind"
---

<objective>
Harden the primary export scripts on the active publish path.

Purpose: make the main CANSLIM export and dashboard export safe under concurrent runs, schema changes, and provider failures before orchestration grows more complex.
Output: `export_canslim.py` and `export_dashboard_data.py` both use artifact-aware publish helpers, explicit failure summaries, and script-level regression coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-safety-hardening/01-RESEARCH.md
@.planning/phases/01-safety-hardening/01-VALIDATION.md
@publish_safety.py
@export_canslim.py
@export_dashboard_data.py
@tests/test_publish_safety.py
@tests/test_export_schema.py
@tests/test_primary_publish_path.py

<interfaces>
Use the shared helper from 01-01 exactly as implemented:

```python
from publish_safety import (
    load_artifact_json,
    validate_resume_stock_entry,
    publish_artifact_bundle,
    PublishValidationError,
    PublishTransactionError,
)
```

Primary summary payloads must validate separately from stock artifacts:

```python
summary_payload = {
    "schema_version": "1.0",
    "artifact_kind": "update_summary",
    "run_id": "...",
    "generated_at": "...",
    "status": "success|failed",
    "stats": {
        "retry_attempts": 0,
        "retry_failures": 0,
        "resume_rejected": 0,
        "stock_failures": 0,
    },
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route `export_canslim.py` through artifact-aware resume validation and bundle publish</name>
  <files>export_canslim.py, tests/test_primary_publish_path.py</files>
  <behavior>
    - Test 1: resume reads the existing artifact through `load_artifact_json(...)` and reprocesses stocks whose saved records are missing required nested fields such as `canslim.grid_strategy` or `canslim.mansfield_rs`.
    - Test 2: retry attempts and final provider failures are counted and surfaced in an explicit failure summary instead of disappearing into silent suppression paths.
    - Test 3: final publish writes `docs/data.json` and `docs/update_summary.json` through one bundle publish call and re-raises publish failures.
  </behavior>
  <action>Replace raw `docs/data.json` reads/writes in `export_canslim.py` with `load_artifact_json(...)` and `publish_artifact_bundle(...)`. Before skipping a resumed stock, call `validate_resume_stock_entry(...)`; if validation fails, log the incompatibility, increment a `resume_rejected` counter, and force a rebuild for that ticker. Audit the active publish-path failure suppression in this file: convert bare `except:` blocks and swallowed retry failures into explicit exception handling that records retry attempts, exhausted retries, and per-stock failure counts. Publish a validated `update_summary.json` alongside `data.json` so the maintainer can see retry/failure outcomes from the main export path, and re-raise final publish/transaction errors so GitHub Actions fails loudly.</action>
  <verify>
    <automated>python3 -m py_compile export_canslim.py publish_safety.py && PYTHONPATH=. pytest tests/test_publish_safety.py tests/test_export_schema.py tests/test_primary_publish_path.py -k export_canslim -q</automated>
  </verify>
  <done>`export_canslim.py` no longer trusts resume-by-existence, no longer silently suppresses active-path publish failures, and publishes failure summary metadata with its main artifact bundle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden `export_dashboard_data.py` with artifact-aware validation and script smoke coverage</name>
  <files>export_dashboard_data.py, tests/test_primary_publish_path.py</files>
  <behavior>
    - Test 1: dashboard export publishes through the artifact-aware helper without assuming every payload is stock-shaped in the same way as `update_summary.json`.
    - Test 2: missing parquet input, validation failure, or publish failure logs the exception and exits non-zero instead of returning a silently incomplete artifact.
  </behavior>
  <action>Update `export_dashboard_data.py` to call the shared helper with the correct `artifact_kind` for its output and to publish through the bundle-safe API even when only one live artifact is being promoted. Preserve the current output shape where required by frontend consumers, but add the Phase 1 metadata envelope (`schema_version`, `artifact_kind`, `run_id`, `generated_at`) so validation stays explicit. Replace silent or return-only failure paths with `logger.exception(...)` plus re-raise. Extend `tests/test_primary_publish_path.py` with a smoke/regression case that exercises the script's publish path against temp files and asserts it uses the artifact-aware contract rather than the stock-only helper assumptions.</action>
  <verify>
    <automated>python3 -m py_compile export_dashboard_data.py publish_safety.py && PYTHONPATH=. pytest tests/test_primary_publish_path.py -k export_dashboard_data -q</automated>
  </verify>
  <done>`export_dashboard_data.py` uses the shared artifact-aware publish contract, fails loudly on bad input/publish errors, and has script-level regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external API/parquet input→exporter | Remote or cached market data becomes published static JSON |
| exporter→publish bundle | Primary exporters promote live artifacts that users and later scripts consume |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | T | `export_canslim.py` resume/load path | mitigate | Validate existing payload and each resumed stock record before any skip decision. |
| T-01-06 | R | `export_canslim.py` retry/failure handling | mitigate | Record retry attempts, exhausted retries, and stock failures in explicit summary payload fields and logs. |
| T-01-07 | D | primary publish transaction | mitigate | Promote `data.json` and `update_summary.json` through one bundle-safe publish call. |
| T-01-08 | E | `export_dashboard_data.py` payload contract | mitigate | Use artifact-aware validation and fail closed on invalid input or publish errors. |
</threat_model>

<verification>
Run `python3 -m py_compile export_canslim.py export_dashboard_data.py publish_safety.py`, `PYTHONPATH=. pytest tests/test_publish_safety.py tests/test_export_schema.py tests/test_primary_publish_path.py -q`, and confirm the `export_canslim` cases assert retry/failure summary reporting.
</verification>

<success_criteria>
- Primary exporters use the shared artifact-aware helper instead of raw live-file writes.
- Resume compatibility is enforced at the stock-record level, not just top-level schema metadata.
- Active-path retries/failures surface in explicit summaries and non-zero failures instead of silent suppression.
- Script-level smoke/regression checks exist for both modified primary export scripts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-safety-hardening/01-02-SUMMARY.md`
</output>
