---
phase: 03-rotating-batch-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .orchestration/rotation_state.json
  - orchestration_state.py
  - provider_policies.py
  - tests/conftest.py
  - tests/test_rotation_state.py
  - tests/test_provider_policies.py
autonomous: true
requirements:
  - ORCH-03
  - ORCH-05
must_haves:
  truths:
    - "ORCH-03: file-based orchestration state survives reruns with cursor, freshness, retry_queue, and in_progress data."
    - "ORCH-05: requests, FinMind, TEJ, and yfinance share explicit retry/backoff policy contracts without lowering the default 1000-symbol target."
  artifacts:
    - path: ".orchestration/rotation_state.json"
      provides: "Tracked seed state for durable repo-backed orchestration persistence"
      contains: "schema_version"
    - path: "orchestration_state.py"
      provides: "Atomic JSON state load/save and retry queue helpers"
      exports: ["load_rotation_state", "save_rotation_state", "enqueue_retry_failure"]
    - path: "provider_policies.py"
      provides: "Provider policy table and backoff helpers"
      exports: ["ProviderPolicy", "get_provider_policy", "compute_backoff_seconds"]
    - path: "tests/test_rotation_state.py"
      provides: "Regression coverage for atomic state persistence and queue durability"
      contains: "load_rotation_state"
    - path: "tests/test_provider_policies.py"
      provides: "Regression coverage for provider retry/backoff semantics"
      contains: "get_provider_policy"
  key_links:
    - from: "tests/test_rotation_state.py"
      to: "orchestration_state.py"
      via: "imported state helpers"
      pattern: "load_rotation_state|save_rotation_state|enqueue_retry_failure"
    - from: "tests/test_provider_policies.py"
      to: "provider_policies.py"
      via: "policy table assertions"
      pattern: "ProviderPolicy|get_provider_policy|compute_backoff_seconds"
---

<objective>
Establish the durable state and provider-policy foundations Phase 3 needs before any rotation logic touches `export_canslim.py`.

Purpose: Lock in file-based persistence, schema validation, and shared retry/backoff contracts so later plans can implement rotation and provider integration without inventing new seams.
Output: Seeded orchestration state, state helpers, provider policy helpers, and passing targeted pytest coverage.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-rotating-batch-orchestration/03-CONTEXT.md
@.planning/phases/03-rotating-batch-orchestration/03-RESEARCH.md
@.planning/phases/03-rotating-batch-orchestration/03-VALIDATION.md
@publish_safety.py
@export_canslim.py
@tests/test_publish_safety.py

<interfaces>
From publish_safety.py:
```python
class PublishValidationError(Exception): ...
class PublishTransactionError(Exception): ...
def publish_artifact_bundle(bundle: dict[str, dict], *, lock_path: str = "docs/.publish.lock", backup_dir: str = "backups/last_good", logger=None, json_default=None) -> dict: ...
```

From export_canslim.py:
```python
SCHEMA_VERSION = "1.0"
class CanslimEngine:
    def _fetch_with_retry(self, url: str, params: Dict = None, max_retries: int = 3) -> Optional[requests.Response]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create durable orchestration-state contracts and regression coverage</name>
  <read_first>
- .planning/phases/03-rotating-batch-orchestration/03-CONTEXT.md
- .planning/phases/03-rotating-batch-orchestration/03-RESEARCH.md
- orchestration_state.py
- publish_safety.py
- tests/test_publish_safety.py
  </read_first>
  <files>.orchestration/rotation_state.json, orchestration_state.py, tests/conftest.py, tests/test_rotation_state.py</files>
  <behavior>
    - Test 1: `load_rotation_state()` returns a schema-valid default payload when `.orchestration/rotation_state.json` is missing or freshly seeded.
    - Test 2: `save_rotation_state()` writes atomically with `os.replace` semantics and preserves `current_batch_index`, `rotation_generation`, `retry_queue`, `freshness`, and `in_progress`.
    - Test 3: `enqueue_retry_failure()` persists failed-symbol metadata across reloads instead of leaving failures in memory only.
  </behavior>
  <action>Create `.orchestration/rotation_state.json` as the only tracked durable state file and implement `orchestration_state.py` around it with a schema-versioned dataclass or typed dict. Use file-based JSON only (no database) per ORCH-03 and brownfield constraints. Include exact fields for `schema_version`, `current_batch_index`, `rotation_generation`, `retry_queue`, `freshness`, `in_progress`, and `last_completed_batch`; reject malformed payloads loudly with `ValueError`/`PublishValidationError` semantics rather than repairing them silently. Save via temp file + `os.replace` to mitigate tampering/corruption risk, mirroring the repo’s publish-safety pattern. Seed the tracked JSON file with an empty-but-valid payload so the workflow can commit later updates deterministically.</action>
  <acceptance_criteria>
- `grep -q "def load_rotation_state" orchestration_state.py`
- `grep -q "def save_rotation_state" orchestration_state.py`
- `grep -q "def enqueue_retry_failure" orchestration_state.py`
- `python - <<'PY'\nimport json\npayload=json.load(open('.orchestration/rotation_state.json'))\nassert {'schema_version','current_batch_index','rotation_generation','retry_queue','freshness','in_progress'}.issubset(payload)\nPY`
- `PYTHONPATH=. pytest -q tests/test_rotation_state.py` exits 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest -q tests/test_rotation_state.py</automated>
  </verify>
  <done>State helpers can load, validate, atomically save, and reload the tracked rotation state without losing cursor or retry metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define provider retry/backoff policy contracts with executable tests</name>
  <read_first>
- .planning/phases/03-rotating-batch-orchestration/03-CONTEXT.md
- .planning/phases/03-rotating-batch-orchestration/03-RESEARCH.md
- provider_policies.py
- export_canslim.py
- finmind_processor.py
- tej_processor.py
  </read_first>
  <files>provider_policies.py, tests/test_provider_policies.py</files>
  <behavior>
    - Test 1: `get_provider_policy()` returns explicit policies for `requests`, `finmind`, `tej`, and `yfinance`.
    - Test 2: `compute_backoff_seconds()` grows deterministically by attempt and never returns negative or zero values for retryable attempts.
    - Test 3: helper metadata preserves the default 1000 non-core daily target rather than reducing the cap up front.
  </behavior>
  <action>Create `provider_policies.py` with a small, explicit policy table for `requests`, `finmind`, `tej`, and `yfinance`, including provider name, max attempts, base backoff seconds, retryable exception classes or status markers, and any due-time metadata needed later by the retry queue. Keep the shared contract pure and testable: deterministic helpers only, no live network calls. Encode D-07 directly by exposing a `DEFAULT_NON_CORE_DAILY_BUDGET = 1000` constant or equivalent contract that later plans must consume unchanged; provider throttling/backoff must manage quota pressure instead of lowering the target. Fail on unknown providers instead of falling back silently.</action>
  <acceptance_criteria>
- `grep -q "DEFAULT_NON_CORE_DAILY_BUDGET = 1000" provider_policies.py`
- `grep -q "class ProviderPolicy" provider_policies.py`
- `grep -q "def get_provider_policy" provider_policies.py`
- `grep -q "def compute_backoff_seconds" provider_policies.py`
- `PYTHONPATH=. pytest -q tests/test_provider_policies.py` exits 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest -q tests/test_provider_policies.py</automated>
  </verify>
  <done>Provider policies are codified in one repo-local module with passing tests and are ready for reuse by `export_canslim.py`, `finmind_processor.py`, and `tej_processor.py`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script runtime → `.orchestration/rotation_state.json` | untrusted/crash-prone writes cross into durable repo state |
| script runtime → external providers | retry policy controls requests sent to FinMind, TEJ, yfinance, and requests-based endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `.orchestration/rotation_state.json` | mitigate | Use schema validation plus temp-file writes and `os.replace`; reject malformed state as a blocking error instead of mutating it silently. |
| T-03-02 | D | `provider_policies.py` consumers | mitigate | Encode explicit per-provider max attempts/backoff values and test them; no implicit infinite retry loops. |
| T-03-03 | R | retry metadata | mitigate | Persist retry queue fields and due-time metadata in state so failures are attributable across reruns. |
</threat_model>

<verification>
Run `PYTHONPATH=. pytest -q tests/test_rotation_state.py tests/test_provider_policies.py` after both tasks.
</verification>

<success_criteria>
- `.orchestration/rotation_state.json` exists with the agreed schema keys.
- `orchestration_state.py` and `provider_policies.py` export the named contracts.
- Targeted state/provider pytest suites pass with `PYTHONPATH=.`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rotating-batch-orchestration/03-01-SUMMARY.md`
</output>
