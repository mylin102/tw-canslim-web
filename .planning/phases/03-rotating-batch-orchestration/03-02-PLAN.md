---
phase: 03-rotating-batch-orchestration
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - rotation_orchestrator.py
  - orchestration_state.py
  - tests/test_rotation_orchestrator.py
  - tests/test_rotation_state.py
autonomous: true
requirements:
  - ORCH-02
  - ORCH-03
  - ORCH-04
must_haves:
  truths:
    - "ORCH-02: non-core symbols are partitioned into three deterministic groups from the stable sorted non-core universe."
    - "ORCH-03: future groups recompute deterministically when core membership changes, while open batches keep their frozen symbol list."
    - "ORCH-04: interrupted runs resume only remaining symbols from the in-progress batch and do not rebuild already-finished work."
  artifacts:
    - path: "rotation_orchestrator.py"
      provides: "Deterministic partitioning, retry-first plan assembly, and finalize/resume seams"
      exports: ["build_rotation_groups", "build_daily_plan", "write_in_progress", "finalize_success", "finalize_failure"]
    - path: "tests/test_rotation_orchestrator.py"
      provides: "Regression coverage for partitioning, generation churn, retry-first scheduling, and resume"
      contains: "build_daily_plan"
    - path: "orchestration_state.py"
      provides: "State mutations required by orchestrator finalization"
      exports: ["load_rotation_state", "save_rotation_state", "enqueue_retry_failure"]
  key_links:
    - from: "rotation_orchestrator.py"
      to: "core_selection.py"
      via: "non-core membership derived from selector core_set"
      pattern: "core_set|sorted"
    - from: "rotation_orchestrator.py"
      to: "orchestration_state.py"
      via: "load/save and queue persistence"
      pattern: "load_rotation_state|save_rotation_state|enqueue_retry_failure"
    - from: "tests/test_rotation_orchestrator.py"
      to: "rotation_orchestrator.py"
      via: "partition/generation/resume assertions"
      pattern: "build_rotation_groups|build_daily_plan|finalize_success"
---

<objective>
Implement the deterministic rotation state machine that turns Phase 2 core selection into a 3-day non-core schedule with durable resume behavior.

Purpose: Deliver the actual ORCH-02/03/04 mechanics before wiring them into the export path.
Output: Rotation orchestration helpers with passing partitioning, retry-first, and resume regressions.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-rotating-batch-orchestration/03-CONTEXT.md
@.planning/phases/03-rotating-batch-orchestration/03-RESEARCH.md
@.planning/phases/03-rotating-batch-orchestration/03-VALIDATION.md
@.planning/phases/02-dynamic-core-selection/02-02-SUMMARY.md
@.planning/phases/02-dynamic-core-selection/02-03-SUMMARY.md
@core_selection.py
@orchestration_state.py
@provider_policies.py

<interfaces>
From core_selection.py:
```python
class CoreSelectionResult:
    core_symbols: list[str]
    bucket_counts: dict[str, int]
    @property
    def core_set(self) -> set[str]: ...
```

From Plan 03-01 contracts:
```python
def load_rotation_state(path: str | Path = ".orchestration/rotation_state.json") -> dict: ...
def save_rotation_state(state: dict, path: str | Path = ".orchestration/rotation_state.json") -> None: ...
def enqueue_retry_failure(state: dict, *, symbol: str, provider: str, error: str, due_at: str | None = None) -> dict: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic 3-way partitioning and generation-aware batch planning</name>
  <read_first>
- .planning/phases/03-rotating-batch-orchestration/03-CONTEXT.md
- .planning/phases/03-rotating-batch-orchestration/03-RESEARCH.md
- core_selection.py
- orchestration_state.py
- rotation_orchestrator.py
  </read_first>
  <files>rotation_orchestrator.py, tests/test_rotation_orchestrator.py</files>
  <behavior>
    - Test 1: `build_rotation_groups()` returns exactly 3 groups from `sorted(all_symbols - core_set)` using stable symbol ordering per D-02.
    - Test 2: a changed `core_set` produces a different generation fingerprint and recomputed future groups per D-03.
    - Test 3: if `in_progress` exists, `build_daily_plan()` keeps the frozen symbol list for resume instead of regrouping that open batch.
  </behavior>
  <action>Create `rotation_orchestrator.py` as a pure helper module, not a new service. Implement `build_rotation_groups(...)`, `compute_rotation_generation(...)`, and `build_daily_plan(...)` against Phase 2 selector output: derive `non_core_sorted = sorted(symbol for symbol in all_symbols if symbol not in core_set)`, partition it into three stable groups, and persist a deterministic generation fingerprint from that ordered list. Honor D-03 by recomputing future groups whenever membership changes, but if the state contains an unfinished `in_progress` batch, return that frozen ordered symbol list until it completes. Do not use date-based offsets or hashing.</action>
  <acceptance_criteria>
- `grep -q "def build_rotation_groups" rotation_orchestrator.py`
- `grep -q "def compute_rotation_generation" rotation_orchestrator.py`
- `grep -q "def build_daily_plan" rotation_orchestrator.py`
- `PYTHONPATH=. pytest -q tests/test_rotation_orchestrator.py -k "partition or generation"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest -q tests/test_rotation_orchestrator.py -k "partition or generation"</automated>
  </verify>
  <done>The orchestrator can deterministically compute three non-core groups and distinguish between a new generation and an unfinished in-progress batch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add retry-first resume/finalization seams with success-based cursor advancement</name>
  <read_first>
- .planning/phases/03-rotating-batch-orchestration/03-CONTEXT.md
- .planning/phases/03-rotating-batch-orchestration/03-RESEARCH.md
- rotation_orchestrator.py
- orchestration_state.py
- provider_policies.py
- export_canslim.py
  </read_first>
  <files>rotation_orchestrator.py, orchestration_state.py, tests/test_rotation_orchestrator.py, tests/test_rotation_state.py</files>
  <behavior>
    - Test 1: due retry items are scheduled before fresh rotation work per D-06, but the planned batch still reserves enough capacity to preserve 3-day coverage.
    - Test 2: `write_in_progress()` persists selected batch id, generation, ordered symbols, completed symbols, and remaining symbols before processing starts.
    - Test 3: `finalize_success()` advances `current_batch_index` only after scheduled-batch success per D-01, while failed symbols are enqueued and do not block completion forever per D-04/D-05.
  </behavior>
  <action>Extend `rotation_orchestrator.py` and `orchestration_state.py` with explicit step-boundary seams the verifier can call after each task: `load_state`, `build_daily_plan`, `write_in_progress`, `mark_symbol_completed` (or equivalent), `finalize_success`, and `finalize_failure`. Encode D-04/D-05/D-06 concretely: queued retries run first only if they are due, but the fresh scheduled group keeps reserved capacity so the 3-day cycle still advances; failed fresh symbols move into the durable retry queue and the batch can still complete; cursor movement happens only inside `finalize_success()` after the scheduled batch completes and should not happen in `finalize_failure()` or during `write_in_progress()`. Treat schema mismatch or missing in-progress fields as blocking errors.</action>
  <acceptance_criteria>
- `grep -q "def write_in_progress" rotation_orchestrator.py`
- `grep -q "def finalize_success" rotation_orchestrator.py`
- `grep -q "def finalize_failure" rotation_orchestrator.py`
- `PYTHONPATH=. pytest -q tests/test_rotation_state.py tests/test_rotation_orchestrator.py -k "resume or retry or queue or finalize"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest -q tests/test_rotation_state.py tests/test_rotation_orchestrator.py -k "resume or retry or queue or finalize"</automated>
  </verify>
  <done>The orchestrator can freeze an in-progress batch, resume remaining symbols only, queue failures durably, and advance the cursor only after a successful scheduled-batch finalization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selector output → rotation planner | changing core/non-core membership crosses into durable batch scheduling |
| rotation planner → state file | in-progress and retry metadata must survive crashes without corruption |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | `rotation_orchestrator.py` generation handling | mitigate | Persist generation fingerprints and freeze open batches so universe churn cannot reorder an unfinished batch. |
| T-03-05 | D | retry-first planner | mitigate | Reserve scheduled-group capacity while executing due retries first; test starvation scenarios explicitly. |
| T-03-06 | T | cursor advancement | mitigate | Allow cursor changes only in `finalize_success()` after scheduled-batch completion; never on calendar date or write-in-progress. |
</threat_model>

<verification>
Run `PYTHONPATH=. pytest -q tests/test_rotation_state.py tests/test_rotation_orchestrator.py` after both tasks.
</verification>

<success_criteria>
- `rotation_orchestrator.py` exports deterministic partitioning and finalization seams.
- Resume and retry-first tests pass under `PYTHONPATH=.`.
- No test or helper advances the batch cursor before a successful finalization path.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rotating-batch-orchestration/03-02-SUMMARY.md`
</output>
