---
phase: 03-rotating-batch-orchestration
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - export_canslim.py
  - finmind_processor.py
  - tej_processor.py
  - .github/workflows/update_data.yml
  - tests/test_provider_policies.py
  - tests/test_primary_publish_path.py
autonomous: true
requirements:
  - ORCH-02
  - ORCH-04
  - ORCH-05
must_haves:
  truths:
    - "ORCH-05: export/provider calls honor shared retry/backoff policies for requests, FinMind, TEJ, and yfinance."
    - "ORCH-02: normal export runs process queued retries before the fresh rotation batch while preserving 3-day non-core coverage."
    - "ORCH-04: `export_canslim.py` resumes the frozen batch plan and advances the cursor only after publish succeeds."
  artifacts:
    - path: "export_canslim.py"
      provides: "Rotation-aware scan planning, resume, and publish-boundary finalization"
      contains: "build_daily_plan"
    - path: "finmind_processor.py"
      provides: "FinMind calls routed through shared provider policy wrapper"
      contains: "get_provider_policy"
    - path: "tej_processor.py"
      provides: "TEJ calls routed through shared provider policy wrapper"
      contains: "get_provider_policy"
    - path: ".github/workflows/update_data.yml"
      provides: "Scheduled workflow persists `.orchestration/rotation_state.json` between runs"
      contains: ".orchestration/rotation_state.json"
    - path: "tests/test_primary_publish_path.py"
      provides: "Regression coverage for rotation-aware export resume and publish ordering"
      contains: "rotation"
  key_links:
    - from: "export_canslim.py"
      to: "rotation_orchestrator.py"
      via: "daily plan assembly and finalization"
      pattern: "build_daily_plan|write_in_progress|finalize_success|finalize_failure"
    - from: "export_canslim.py"
      to: "provider_policies.py"
      via: "shared retry/backoff wrapper"
      pattern: "get_provider_policy|compute_backoff_seconds"
    - from: ".github/workflows/update_data.yml"
      to: ".orchestration/rotation_state.json"
      via: "git add on scheduled commit"
      pattern: "rotation_state.json"
---

<objective>
Wire the new rotation and provider-policy foundations into the real export path and scheduled workflow without breaking Phase 1 publish safety.

Purpose: Deliver the observable daily behavior for Phase 3 in the brownfield runtime that already drives GitHub Pages.
Output: Rotation-aware `export_canslim.py`, shared provider-policy usage across data sources, scheduled-state persistence, and passing integration regressions.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-rotating-batch-orchestration/03-CONTEXT.md
@.planning/phases/03-rotating-batch-orchestration/03-RESEARCH.md
@.planning/phases/03-rotating-batch-orchestration/03-VALIDATION.md
@.planning/phases/01-safety-hardening/01-03-SUMMARY.md
@.planning/phases/02-dynamic-core-selection/02-03-SUMMARY.md
@rotation_orchestrator.py
@orchestration_state.py
@provider_policies.py
@export_canslim.py
@finmind_processor.py
@tej_processor.py
@.github/workflows/update_data.yml

<interfaces>
From export_canslim.py:
```python
selection = build_core_universe(...)
scan_list = selection.core_symbols + [t for t in all_t if t not in selection.core_set][:2000]
validate_resume_stock_entry(t, stock_entry, schema_version=SCHEMA_VERSION)
self._publish_snapshot()
```

From publish_safety.py:
```python
def validate_resume_stock_entry(stock_id: str, stock_entry: dict, *, schema_version: str = DEFAULT_SCHEMA_VERSION, required_paths: tuple[str, ...] = REQUIRED_STOCK_PATHS) -> None: ...
def publish_artifact_bundle(bundle: dict[str, dict], *, lock_path: str = "docs/.publish.lock", backup_dir: str = "backups/last_good", logger=None, json_default=None) -> dict: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route provider calls through the shared policy layer</name>
  <read_first>
- provider_policies.py
- export_canslim.py
- finmind_processor.py
- tej_processor.py
- tests/test_provider_policies.py
- .planning/phases/03-rotating-batch-orchestration/03-RESEARCH.md
  </read_first>
  <files>export_canslim.py, finmind_processor.py, tej_processor.py, tests/test_provider_policies.py</files>
  <behavior>
    - Test 1: requests-based retry paths in `export_canslim.py` record attempts/failures through the shared policy contract instead of ad hoc sleeps.
    - Test 2: FinMind calls use the shared policy wrapper around `DataLoader` fetches and expose retry exhaustion cleanly.
    - Test 3: TEJ calls route `tejapi.get(...)` through one internal wrapper so provider backoff is enforced consistently across tables.
  </behavior>
  <action>Integrate `provider_policies.py` into all Phase 3 provider touchpoints. In `export_canslim.py`, replace the hard-coded exponential sleep loop inside `_fetch_with_retry()` with the shared helper while preserving explicit failure stats. In `finmind_processor.py`, wrap `fetch_institutional_investors()` in the shared FinMind policy instead of one-shot calls. In `tej_processor.py`, add one internal helper (for example `_tej_get_with_policy(...)`) and route every `tejapi.get(...)` call through it so TEJ retry/backoff behavior is consistent. Keep the repo brownfield: no new orchestration framework, no hidden global state, and no lowering of the 1000-symbol target to compensate for quota pressure.</action>
  <acceptance_criteria>
- `grep -q "get_provider_policy" export_canslim.py`
- `grep -q "get_provider_policy" finmind_processor.py`
- `grep -q "get_provider_policy" tej_processor.py`
- `PYTHONPATH=. pytest -q tests/test_provider_policies.py` exits 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest -q tests/test_provider_policies.py</automated>
  </verify>
  <done>Requests, FinMind, and TEJ provider calls now share one tested retry/backoff contract and continue surfacing retry statistics explicitly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the static non-core tail with rotation-aware export planning and persist state in the scheduled workflow</name>
  <read_first>
- .planning/phases/03-rotating-batch-orchestration/03-CONTEXT.md
- .planning/phases/03-rotating-batch-orchestration/03-RESEARCH.md
- export_canslim.py
- rotation_orchestrator.py
- orchestration_state.py
- publish_safety.py
- .github/workflows/update_data.yml
- tests/test_primary_publish_path.py
  </read_first>
  <files>export_canslim.py, .github/workflows/update_data.yml, tests/test_primary_publish_path.py</files>
  <behavior>
    - Test 1: normal runs attempt due retry-queue symbols before fresh rotation work per D-06, but still reserve enough scheduled-batch capacity to preserve 3-day coverage.
    - Test 2: `export_canslim.py` writes `in_progress` state before processing the scheduled batch and resumes only remaining symbols after interruption.
    - Test 3: `current_batch_index` advances only after `_publish_snapshot()` succeeds for the scheduled batch per D-01, while failed symbols are queued durably and do not block completion forever per D-04/D-05.
  </behavior>
  <action>Modify `export_canslim.py` to stop using `selection.core_symbols + first 2000 remaining`. After `build_core_universe(...)`, load `.orchestration/rotation_state.json`, build the daily non-core plan through `rotation_orchestrator.py`, and compose the final worklist as `core_symbols + due_retries + scheduled_batch_slice`. Write the in-progress checkpoint before the first scheduled-batch provider call, preserve the existing 50-stock incremental publish cadence, and call `finalize_success()` only after the final `_publish_snapshot()` for the scheduled batch succeeds. If the run aborts or publish fails, call `finalize_failure()` and leave the cursor unchanged. Update `.github/workflows/update_data.yml` so scheduled commits include `.orchestration/rotation_state.json`; use the fixed path only, not globbing. Do not add Phase 4 publish/freshness/search work.</action>
  <acceptance_criteria>
- `grep -q "build_daily_plan" export_canslim.py`
- `grep -q "write_in_progress" export_canslim.py`
- `grep -q "finalize_success" export_canslim.py`
- `grep -q ".orchestration/rotation_state.json" .github/workflows/update_data.yml`
- `PYTHONPATH=. pytest -q tests/test_primary_publish_path.py -k "rotation or resume or retry"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=. pytest -q tests/test_primary_publish_path.py -k "rotation or resume or retry"</automated>
  </verify>
  <done>The scheduled export path now uses retry-first deterministic rotation with durable resume state, and the workflow persists that state across GitHub Actions runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external providers → export/provider wrappers | degraded providers can trigger retry storms or inconsistent failure accounting |
| export runtime → published docs + rotation state | publish success/failure determines whether the batch cursor may advance |
| GitHub Actions commit step → repo state file | workflow persistence determines whether rotation state survives the next run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | D | provider integrations in `export_canslim.py`, `finmind_processor.py`, `tej_processor.py` | mitigate | Route all provider calls through the shared policy helper with explicit max attempts/backoff and test retry exhaustion paths. |
| T-03-08 | T | `export_canslim.py` cursor finalization | mitigate | Finalize the rotation cursor only after `_publish_snapshot()` succeeds; on failure, leave `current_batch_index` unchanged and persist failure state. |
| T-03-09 | T | `.github/workflows/update_data.yml` commit step | mitigate | Stage the fixed `.orchestration/rotation_state.json` path explicitly so durable state survives scheduled reruns without exposing arbitrary path writes. |
</threat_model>

<verification>
Run `PYTHONPATH=. pytest -q tests/test_provider_policies.py tests/test_primary_publish_path.py` after both tasks.
</verification>

<success_criteria>
- The export path no longer uses the static `[:2000]` non-core tail.
- Provider retry/backoff behavior is shared across requests, FinMind, and TEJ code paths.
- Scheduled workflow commits `.orchestration/rotation_state.json`.
- Targeted Phase 3 integration tests pass with `PYTHONPATH=.`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rotating-batch-orchestration/03-03-SUMMARY.md`
</output>
