---
phase: 05-revenue-feature-pipeline
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [feature_pipeline.py, quick_auto_update_enhanced.py, api/stock_features.json, api/ranking.json]
autonomous: true
requirements: [REV-FEAT-04, REV-FEAT-05]

must_haves:
  truths:
    - "Can compute features for the core stock universe and export to JSON files"
    - "Produced JSON files match the schema from the pipeline spec"
    - "The update process includes the feature pipeline as an automated step"
  artifacts:
    - path: "feature_pipeline.py"
      provides: "Feature engineering and export orchestration"
    - path: "api/stock_features.json"
      provides: "Standardized revenue feature output"
    - path: "api/ranking.json"
      provides: "Standardized scoring/ranking output"
  key_links:
    - from: "feature_pipeline.py"
      to: "revenue_analyzer.py"
      via: "Call calculate_revenue_features for each stock"
    - from: "feature_pipeline.py"
      to: "api/stock_features.json"
      via: "JSON serialization of computed results"
---

<objective>
Integrate the revenue feature pipeline into the update workflow and export standardized JSON files to the api/ directory.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@docs/VAN_FEATURE_pipiline.md
@.planning/phases/05-revenue-feature-pipeline/05-CONTEXT.md
@.planning/phases/05-revenue-feature-pipeline/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Feature Pipeline Orchestrator</name>
  <files>feature_pipeline.py</files>
  <action>
    Create `feature_pipeline.py` that:
    - Sets up a list of target stocks (e.g. from `core_selection_config.json` or active symbols).
    - Iterates through stocks:
      - Fetches or loads monthly revenue data for each.
      - Uses `revenue_analyzer` to compute features.
      - Aggregates results into a list of feature dicts and a list of ranking dicts.
    - Exports the aggregated results to `api/stock_features.json` and `api/ranking.json`.
    - Includes `feature_version: "v1.0"` and `updated_at`.
    - Handles target directory creation (`mkdir -p api`).
  </action>
  <verify>
    `python3 feature_pipeline.py --test-mode` (test mode should process 1-2 stocks and verify output exist)
  </verify>
  <done>Pipeline orchestrator implemented and functional.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate into Update Workflow</name>
  <files>quick_auto_update_enhanced.py</files>
  <action>
    - Add a call to `feature_pipeline.run()` in the main flow of `quick_auto_update_enhanced.py`, typically after the primary data update and before the final sync/summary.
    - Ensure it's guarded by error handling so it doesn't break the entire update if feature computation fails for one stock.
  </action>
  <verify>
    Check `quick_auto_update_enhanced.py` for correct integration point.
  </verify>
  <done>Feature pipeline integrated into the automated update workflow.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Full Revenue Feature Pipeline</what-built>
  <how-to-verify>
    1. Run `python3 feature_pipeline.py`
    2. Verify `api/stock_features.json` exists and contains correct keys for a sample stock (e.g. "2330").
    3. Verify `api/ranking.json` exists and contains `revenue_score`.
    4. Run `python3 quick_auto_update_enhanced.py --symbols 2330` and check if the API files are updated.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline → File System | Writes output files to root /api/ directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | I | api/stock_features.json | mitigate | Ensure atomic write or at least write with proper error handling to prevent partial JSON files. |
</threat_model>

<success_criteria>
- `api/stock_features.json` and `api/ranking.json` are produced automatically after an update.
- The output format matches the spec provided in `docs/VAN_FEATURE_pipiline.md`.
- Pipeline can be run standalone or as part of the main update workflow.
</success_criteria>
