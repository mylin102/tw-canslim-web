---
phase: 06-revenue-driven-selection-alpha
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [export_canslim.py, tests/test_export_revenue.py]
autonomous: true
requirements: [REV-SEL-02, REV-SEL-03, REV-SEL-04]
must_haves:
  truths:
    - "CanslimEngine correctly loads docs/api/stock_features.json"
    - "Revenue tags 'rev_acc' and 'rev_strong' appear in leaders.json"
    - "composite_score in leaders.json is blended with revenue score"
  artifacts:
    - path: "export_canslim.py"
      provides: "Updated export script and engine logic"
    - path: "data/leaders.json"
      provides: "Revenue-aware alpha payload"
  key_links:
    - from: "export_canslim.py"
      to: "core_selection.py"
      via: "Function calls to build_core_universe with revenue data"
---

<objective>
Update export_canslim.py (the main engine and alpha exporter) to integrate revenue features.

Purpose: Impact the final trading universe and position scaling with revenue alpha.
Output: Alpha exporter that blends revenue into composite scores and attaches tags.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-revenue-driven-selection-alpha/06-01-SUMMARY.md
@core_selection.py
@export_canslim.py
@docs/api/stock_features.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CanslimEngine.run to load revenue features</name>
  <files>export_canslim.py</files>
  <action>
    - Load docs/api/stock_features.json at start of CanslimEngine.run.
    - Create a lookup map {symbol: feature_dict}.
    - In the stock analysis loop, attach relevant revenue features (revenue_score, rev_accelerating, rev_strong) to the stock_data["canslim"] dict.
    - Pass the revenue path to build_core_universe call.
  </action>
  <verify>
    <automated>grep -n "stock_features.json" export_canslim.py</automated>
  </verify>
  <done>Revenue features are attached to stock_data.</done>
</task>

<task type="auto">
  <name>Task 2: Update _export_leaders_json to blend revenue score</name>
  <files>export_canslim.py</files>
  <action>
    - Update the composite_score calculation in _export_leaders_json: 
      composite_score = 0.7 * (canslim_score / 100.0) + 0.3 * (revenue_score / 6.0).
    - Add tags "rev_acc" and "rev_strong" if rev_accelerating and rev_strong are True, respectively.
    - Ensure it handles missing revenue data (default score 0, default false).
  </action>
  <verify>
    <automated>grep -n "rev_acc" export_canslim.py</automated>
  </verify>
  <done>Alpha export payload includes revenue score and tags.</done>
</task>

<task type="auto">
  <name>Task 3: Integration test for alpha export</name>
  <files>tests/test_export_revenue.py</files>
  <action>
    - Create a test that runs _export_leaders_json with a mock selection and mock output_data containing revenue features.
    - Assert that data/leaders.json contains the expected composite_score and tags.
  </automated>
  <verify>
    <automated>pytest tests/test_export_revenue.py</automated>
  </verify>
  <done>Integration test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system → CanslimEngine | docs/api/stock_features.json is untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | stock_features.json | mitigate | Validate JSON structure and field types during load in export_canslim.py. |
</threat_model>

<success_criteria>
- leaders.json reflects revenue score in composite_score.
- rev_acc and rev_strong tags appear correctly.
- build_core_universe is called with revenue path.
</success_criteria>

<output>
After completion, create `.planning/phases/06-revenue-driven-selection-alpha/06-02-SUMMARY.md`
</output>
