---
phase: 06-revenue-driven-selection-alpha
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [core_selection.py, tests/test_revenue_selection.py]
autonomous: true
requirements: [REV-SEL-01, REV-SEL-03]
must_haves:
  truths:
    - "core_selection.py can load revenue features from docs/api/stock_features.json"
    - "A new bucket revenue_alpha_leaders is identified for stocks with score >= 5 and accelerating == True"
    - "RankedCandidate sort key prioritizes revenue_score over mansfield_rs"
  artifacts:
    - path: "core_selection.py"
      provides: "Revenue-aware selection logic"
    - path: "tests/test_revenue_selection.py"
      provides: "Unit tests for revenue-driven selection"
  key_links:
    - from: "core_selection.py"
      to: "docs/api/stock_features.json"
      via: "json.load in load_selector_inputs"
---

<objective>
Update core_selection.py to integrate revenue features into the selection buckets and ranking logic using TDD.

Purpose: Prioritize high-revenue-growth stocks in the core universe and ranking.
Output: Revenue-aware selection module and passing unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@core_selection.py
@docs/api/stock_features.json
</context>

<feature>
  <name>Revenue-Driven Core Selection</name>
  <files>core_selection.py, tests/test_revenue_selection.py</files>
  <behavior>
    - load_selector_inputs accepts an optional revenue_path (defaulting to docs/api/stock_features.json).
    - load_selector_inputs returns a new bucket 'revenue_alpha_leaders' containing symbols where revenue_score >= 5 and rev_accelerating is True.
    - RankedCandidate includes a revenue_score field (default 0.0).
    - _ranked_candidate_sort_key uses the tuple: (-revenue_score, -mansfield_rs, volume_rank, symbol).
    - build_core_universe includes 'revenue_alpha_leaders' in BUCKET_ORDER after 'today_signals'.
  </behavior>
  <implementation>
    1. Update RankedCandidate dataclass to add revenue_score: float = 0.0.
    2. Update _ranked_candidate_sort_key to prioritize revenue_score.
    3. Update load_selector_inputs signature to add revenue_path.
    4. In load_selector_inputs, load revenue data from JSON, create a mapping {symbol: (score, accelerating)}.
    5. Filter latest_rows to find revenue_alpha_leaders.
    6. Map revenue_score into RankedCandidate objects.
    7. Update BUCKET_ORDER and build_core_universe to handle the new bucket.
  </implementation>
</feature>

<verification>
<automated>pytest tests/test_revenue_selection.py</automated>
</verification>

<success_criteria>
- Ranked candidates are sorted by revenue_score first.
- revenue_alpha_leaders bucket is correctly populated and prioritized.
- Tests cover edge cases (missing revenue data, malformed JSON).
</success_criteria>

<output>
After completion, create `.planning/phases/06-revenue-driven-selection-alpha/06-01-SUMMARY.md`
</output>
