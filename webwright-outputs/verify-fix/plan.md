# Task
Verify the fix: check that mylin102.github.io/tw-canslim-web now serves populated `api/stock_features.json` and `api/ranking.json` instead of empty `{}`.

# Critical Points
- [ ] CP1: api/stock_features.json 內容為非空物件（包含股票特徵資料）
- [ ] CP2: api/ranking.json 內容為非空物件（包含排名資料）
- [ ] CP3: data.json 和 stock_index.json 等主要資料仍正常
- [ ] CP4: 首頁渲染正常
