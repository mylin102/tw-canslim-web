可以，建議做成 **Chip Intelligence v1**，先放在 `tw-canslim-web`，再輸出給 `tw-trading-unified` 使用。

````md
# tw-canslim-web 改善計畫：Chip Intelligence v1

## 1. 目標

強化 `tw-canslim-web` 的選股品質，加入「股權分散 / 籌碼集中度 / 大戶累積」因子，避免只靠營收、RS、技術分數選股。

核心目的：

- 找出大戶持續累積的股票
- 避免股價上漲但籌碼發散的股票
- 改善 leader ranking 的鑑別度
- 產出更適合 `tw-trading-unified` 消費的 `leaders.json`

---

## 2. 資料來源

優先順序：

1. 官方 TDCC / 政府開放資料
2. 若官方資料格式不穩，再用 norway.twsthr.info 作為 fallback
3. 不建議交易系統直接爬 norway

資料頻率：

- 每週更新一次
- 適合用於中期選股與 regime filter
- 不適合日內交易訊號

---

## 3. 新增資料表

```text
data/chips/tdcc_holders_weekly.csv
````

建議欄位：

```text
date
stock_id
level
holders
shares
percent
```

整理後產生：

```text
data/chips/chip_features.csv
```

欄位：

```text
date
stock_id
holders_total
avg_lots_per_holder
pct_400_plus
pct_600_plus
pct_800_plus
pct_1000_plus
holders_wow
avg_lots_wow
pct_400_plus_4w_delta
pct_1000_plus_4w_delta
chip_accumulation_score
chip_distribution_risk
```

---

## 4. 核心因子設計

### 4.1 大戶集中度

```text
pct_400_plus = 400張以上持股比例
pct_1000_plus = 1000張以上持股比例
```

意義：

* 越高代表籌碼越集中
* 但不能只看絕對值，要看變化

---

### 4.2 大戶累積

```text
big_holder_accumulation =
  pct_400_plus_4w_delta > 0
  AND pct_1000_plus_4w_delta > 0
```

加分條件：

```text
pct_400_plus_4w_delta >= +1.0%
pct_1000_plus_4w_delta >= +0.5%
```

---

### 4.3 散戶退場

```text
retail_exit =
  holders_total_4w_delta < 0
  AND avg_lots_per_holder_4w_delta > 0
```

代表：

* 股東人數下降
* 平均每人張數上升
* 籌碼從散戶往大戶集中

---

### 4.4 籌碼發散風險

```text
distribution_risk =
  price_4w_return > 10%
  AND pct_400_plus_4w_delta < 0
  AND holders_total_4w_delta > 0
```

代表：

* 股價上漲
* 大戶持股下降
* 股東人數增加

這通常是出貨或散戶追高風險。

---

## 5. Chip Score 設計

```text
chip_accumulation_score = 0 ~ 100
```

建議權重：

```text
40%  大戶 400 張以上 4週變化
25%  大戶 1000 張以上 4週變化
20%  股東人數下降
15%  平均張數上升
```

範例：

```python
chip_score =
    40 * norm(pct_400_plus_4w_delta) +
    25 * norm(pct_1000_plus_4w_delta) +
    20 * norm(-holders_total_4w_delta_pct) +
    15 * norm(avg_lots_per_holder_4w_delta_pct)
```

---

## 6. 修改 composite_score

目前可改成：

```text
composite_score =
  0.35 * canslim_score
+ 0.25 * rs_score
+ 0.20 * revenue_score
+ 0.20 * chip_score
```

若資料不足：

```text
composite_score =
  0.40 * canslim_score
+ 0.30 * rs_score
+ 0.30 * revenue_score
```

---

## 7. Leader JSON 新增欄位

```json
{
  "stock_id": "2317",
  "name": "鴻海",
  "composite_score": 0.86,
  "canslim_score": 82,
  "rs_score": 88,
  "revenue_score": 75,
  "chip_score": 91,
  "pct_400_plus": 62.4,
  "pct_1000_plus": 48.1,
  "pct_400_plus_4w_delta": 1.8,
  "pct_1000_plus_4w_delta": 0.9,
  "holders_4w_delta_pct": -3.5,
  "chip_status": "ACCUMULATION",
  "distribution_risk": false
}
```

---

## 8. 分類標籤

```text
ACCUMULATION
- 大戶增加
- 股東人數下降
- 平均張數上升

NEUTRAL
- 無明顯變化

DISTRIBUTION_RISK
- 股價漲但大戶減碼
- 股東人數增加

INSUFFICIENT_DATA
- 週資料不足 4 週
```

---

## 9. Dashboard 改善

新增區塊：

```text
籌碼集中度 Top 20
大戶連續累積 Top 20
股價上漲但籌碼發散警示
CANSLIM + Chip 雙高名單
```

每檔股票顯示：

```text
Chip Score
400張以上比例
1000張以上比例
4週變化
股東人數變化
Chip Status
```

---

## 10. 與 tw-trading-unified 整合

`tw-trading-unified` 不直接爬資料，只讀：

```text
leaders.json
watchlist.json
chip_features.json
```

可用於：

```text
- 股票池過濾
- 禁止買進 DISTRIBUTION_RISK
- ACCUMULATION 股票提高 ranking
- RISK_OFF 時只允許 chip_score 高的股票進入 SCOUT
```

---

## 11. 實作順序

### Phase 1：資料 ETL

* 建立 TDCC downloader
* 建立 chip parser
* 產生 weekly chip raw data
* 建立 cache，避免重複下載

### Phase 2：Feature Engineering

* 計算 400張以上比例
* 計算 1000張以上比例
* 計算 4週變化
* 計算 chip_score
* 標記 chip_status

### Phase 3：Scoring Integration

* 修改 composite_score
* 新增 chip_score fallback
* 避免資料不足造成 ranking 壓縮

### Phase 4：Dashboard

* 新增籌碼頁籤
* 新增 accumulation / distribution risk list
* 新增 leader card chip 欄位

### Phase 5：Publish

* 更新 GitHub Pages build
* 輸出 leaders.json
* 輸出 chip_features.json
* 加 watchdog 檢查資料完整性

---

## 12. Watchdog

新增 publish quality check：

```text
- chip_features rows > 1000
- chip_score 非空比例 > 80%
- distribution_risk 數量合理
- composite_score 不可全部集中在 0.7~0.8
- leaders.json 必須含 chip_score
```

---

## 13. MVP 判斷

第一版不用做太複雜。

MVP 必須完成：

```text
TDCC weekly data
pct_400_plus
pct_1000_plus
holders_total
avg_lots_per_holder
4w delta
chip_score
chip_status
leaders.json integration
```

先不要做：

```text
法人買賣超整合
券商分點
借券 / 融資券
即時籌碼
AI ranking
```

---

## 14. 結論

`tw-canslim-web` 應該先升級為：

```text
CANSLIM + RS + Revenue + Chip Intelligence
```

這會比單純營收與技術面更適合台股。

最佳定位：

```text
tw-canslim-web = 中期選股與股票池生成器
tw-trading-unified = 執行、風控、交易決策系統


